[package]
name = "rushort"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "signal", "io-util"] }
parking_lot = "0.12"
serde_json = "1"

[profile.release]
lto = "thin"
codegen-units = 1
